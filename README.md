# sdk (NestJS)

Basic NestJS boilerplate. After installing dependencies, run the app in development:

```bash
npm install
npm run start:dev
```

Build and run production:

```bash
npm run build
npm start
```
